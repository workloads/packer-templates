# This is a Terraform-managed file; manual changes will be overwritten.
# see https://github.com/workloads/github-organization/blob/main/templates/workflows/markdown.yml

---

name: "Documentation: Link Checker"

# yamllint disable-line rule:truthy
on:
  pull_request:

jobs:
  markdown:
    name: Markdown
    runs-on: ubuntu-latest
    steps:
      # see https://github.com/actions/checkout
      - name: Checkout Repository
        uses: "checkout@v3.5.0"
        with:
          fetch-depth: 1

      # see https://github.com/gaurav-nelson/github-action-markdown-link-check
      - name: Check Links in Markdown files
        uses: "github-action-markdown-link-check@1.0.15"
        with:
          check-modified-files-only: no
          config-file: ".markdown-link-check.json"
          file-extension: ".md"
          use-verbose-mode: yes
